<aside class="aside">
    <nav class="aside__nav">
        {% for link in [

            {'title':'О нас','href':'about'},
            {'title':'Консультации','href':''},
            {'title':'Мероприятия','href':'events'},
            {'title':'Выставки','href':'show'},
            {'title':'“Выращивание”\nсубъектов МСП','href':'msp'},
            {'title':'Контакты','href':''}

        ] %}
            <a href="growing-{{ link.href }}.html" class="aside__link {% if loop.index == asideActiveId %}active{% endif %}">
                <span>{{ link.title | striptags(true) | escape | nl2br }}</span>
                {{ macros.svgIcon('chevron-right','','','svg-icon') }}
            </a>
        {% endfor %}
    </nav>

    <a href="#" class="aside__btn va-middle">
                    <span>Оформить <br>
                    заявление на <br>
                    получание услуг</span>
    </a>
</aside>